Are you building a timemachine?
Yes.
What is that button for (points at the reverse 5s button)
Hey don't push that
*click*

Are you building a timemachine?
Yes.
What is that button for (points at the reverse 5s button)
Hey don't push that
*click*

Are you building a timemachine?
Yes.
What is that button for (points at the reverse 5s button)
Hey don't push that
*click*

Are you building a timemachine?
Yes.
What is that button for (points at the reverse 5s button)
Hey don't push that
*click*
